database:
  image: mysql:5
  rootPassword: rootpw
  userName: drupal
  userPassword: changeme
  tinyConfiguration: ""
  persistence:
    storageClass: ""

drupal:
  image: drupal:8
  persistence:
    storageClass: ""

imageCredentials: ~
  # registry: server
  # username: username
  # password: password

scheduler: ""
persistence:
  storageClass: ""

dev:
  path: ""

ingress:
  host: ~
  hosts: []
  tls:
    enabled: false
    certManager:
      clusterIssuer: ""
      issuer: ""
  modsecurity:
    enabled: false
    owasp: false
    whitelist: ""
    adminWhitelist: ""